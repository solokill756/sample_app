<% provide :title, t("home.title") %>
<div class="text-center">
    <h1><%= t("home.welcome_to_sample_app") %></h1>
    <p><%= t("home.description") %></p>
    <%= link_to t("home.sign_up_now"), "#", class: "btn btn-primary btn-lg" %>
</div>
